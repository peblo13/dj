# work-backend

Express proxy for Jooble API.

Usage:

1. Copy `.env.example` -> `.env` and add your Jooble API key.
2. npm install
3. npm run dev (or npm start)

Endpoints:
- POST /api/search  -> forwards body to Jooble API and returns results
